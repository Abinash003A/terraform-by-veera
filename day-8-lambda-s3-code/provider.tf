provider "aws" {
  
}

#command tooverwrite zip file
#zip -r lambda_function.zip lambda_function.py
#need to install package for zip command in windows
#choco install zip -y
#otherwise manual delete the zip file and create a new one